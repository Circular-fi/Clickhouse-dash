name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.goos }} ${{ matrix.goarch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            goos: linux
            goarch: amd64

          - runner: macos-latest
            goos: darwin
            goarch: amd64

          - runner: macos-latest
            goos: darwin
            goarch: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute version (strip leading v)
        id: vars
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"

      - name: Build (Linux static musl, embedded static)
        if: matrix.goos == 'linux'
        shell: bash
        run: |
          set -euo pipefail

          docker run --rm \
            -v "$PWD":/work \
            -w /work \
            alpine:3.20 sh -euxc '
              apk add --no-cache ca-certificates build-base cmake ninja git

              cmake -S src -B build -G Ninja \
                -DCMAKE_BUILD_TYPE=Release \
                -DCHDASH_EMBED_STATIC=ON \
                -DCMAKE_EXE_LINKER_FLAGS="-static -static-libstdc++ -static-libgcc -s"

              cmake --build build --target chdash

              file build/chdash
              ldd build/chdash || true
            '

      - name: Install deps (macOS)
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install ninja

      - name: Configure (macOS)
        if: matrix.goos == 'darwin'
        shell: bash
        run: |
          set -euo pipefail

          EXTRA=()
          if [ "${{ matrix.goarch }}" = "amd64" ]; then
            EXTRA+=("-DCMAKE_OSX_ARCHITECTURES=x86_64")
          else
            EXTRA+=("-DCMAKE_OSX_ARCHITECTURES=arm64")
          fi

          cmake -S src -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCHDASH_EMBED_STATIC=ON \
            "${EXTRA[@]}"

      - name: Build (macOS)
        if: matrix.goos == 'darwin'
        run: cmake --build build --target chdash

      - name: Package
        id: package
        shell: bash
        run: |
          set -euo pipefail

          TAR="chdash_${VERSION}_${{ matrix.goos }}_${{ matrix.goarch }}.tar.gz"

          if [ ! -f build/chdash ]; then
            echo "binary not found at build/chdash" >&2
            exit 1
          fi

          # Put ONLY "chdash" at the root of the tar.gz (no build/ prefix, no folder)
          tar -C build -czf "$TAR" chdash

          echo "tar=$TAR" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: chdash_${{ steps.vars.outputs.version }}_${{ matrix.goos }}_${{ matrix.goarch }}
          path: ${{ steps.package.outputs.tar }}
          if-no-files-found: error

  release:
    name: Publish GitHub Release assets
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Flatten
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p out
          find dist -type f -name "*.tar.gz" -exec cp {} out/ \;
          ls -la out

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          files: out/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}